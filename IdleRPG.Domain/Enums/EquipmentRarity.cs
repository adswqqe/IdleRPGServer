namespace IdleRPG.Domain.Enums
{
    /// <summary>
    /// 장비 등급 (버섯키우기 스타일)
    /// </summary>
    public enum EquipmentRarity
    {
        Common = 1,      // 일반 (회색)
        Uncommon = 2,    // 고급 (초록)
        Rare = 3,        // 희귀 (파랑)
        Epic = 4,        // 영웅 (보라)
        Legendary = 5    // 전설 (주황)
    }
}
